## OPEN XQuartz GUI
#!/bin/bash
# Without this, the GUI will not be able to connect to the X server and will fail to launch.
# sudo systemctl set-default multi-user.target
# Put the GUI launching commands here, for example:
# sudo systemctl set-default graphical.target

